// Client-side authentication form validation helpers
document.addEventListener("DOMContentLoaded", () => {
    const forms = document.querySelectorAll("form");
    forms.forEach(form => {
        form.addEventListener("submit", (e) => {
            // Can add custom client-side validation logic if needed
        });
    });
});
